@model zipgame.Models.GameBoard

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Zip Game";
    var rows = Model.Rows;
    var cols = Model.Cols;
}

@section Scripts {
    <script>
        // Pass puzzle information from server to client.
        var boardConfig = {
            rows: @rows,
            cols: @cols,
            cells: [
                @for (int i = 0; i < rows; i++)
                {
                    <text>[</text>
                    for (int j = 0; j < cols; j++)
                    {
                        <text>@Model.Cells[i, j]@((j < cols - 1) ? "," : "")</text>
                    }
                    <text>]</text>
                    if (i < rows - 1) { <text>,</text> }
                }
            ],
            verticalWalls: [
                @for (int i = 0; i < rows; i++)
                {
                    <text>[</text>
                    for (int j = 0; j < cols + 1; j++)
                    {
                        <text>@Model.VerticalWalls[i, j].ToString().ToLower()@((j < cols) ? "," : "")</text>
                    }
                    <text>]</text>
                    if (i < rows - 1) { <text>,</text> }
                }
            ],
            horizontalWalls: [
                @for (int i = 0; i < rows + 1; i++)
                {
                    <text>[</text>
                    for (int j = 0; j < cols; j++)
                    {
                        <text>@Model.HorizontalWalls[i, j].ToString().ToLower()@((j < cols - 1) ? "," : "")</text>
                    }
                    <text>]</text>
                    if (i < rows) { <text>,</text> }
                }
            ]
        };
    </script>
}

<div>
    <h1>@ViewData["Title"]</h1>
    <div id="game-container">
        <canvas id="gameCanvas" width="500" height="500"></canvas>
    </div>
    <div>
        <button id="startOverButton">Start Over</button>
        <button id="newGameButton">New Game</button>
        <span id="timer">Time: 0s</span>
    </div>
    <div id="result"></div>
</div>
